server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zuoyu?useSSL=false&serverTimezone=CTT&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false