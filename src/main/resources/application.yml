server:
  port: 8080
  servlet:
    context-path: /jwt
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zuoyu?useSSL=false&serverTimezone=CTT
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false